# ゲームシステムの概要

このドキュメントは、ゲームの主要なシステムコンポーネントの設計と相互作用について説明します。

## 主要コンポーネント

- **PlayerController**: プレイヤーの入力と移動を管理します。
- **Digger**: 地形を掘削する機能を担当します。
- **VoxelChunk**: 地形を構成するボクセルの塊を管理します。
- **BaseCubePlacer**: 地形（VoxelChunk）を配置する基底クラスです。

---

## 1. PlayerController

プレイヤーの操作と状態を管理する中心的なコンポーネントです。

### 主な機能

- **移動制御**:
    - Unityの新しいInput Systemを利用して、キーボードからの入力を受け取ります。
    - `MoveMode`（`TopDown` / `SideScroller`）に応じて、移動平面を切り替えます。
        - `TopDown`: XZ平面（トップダウン）での移動。
        - `SideScroller`: XY平面（サイドスクロール）での移動。
    - `Rigidbody`の`linearVelocity`を直接操作して、スムーズな移動を実現します。
    - 移動方向に応じてプレイヤーの向きを自動的に変更します。

- **掘削エリアの更新**:
    - プレイヤーの子オブジェクトとして配置された`Digger`コンポーネントと連携します。
    - `digger.UpdateDiggingAreaTransform()`を呼び出し、プレイヤーの向きと`MoveMode`に応じたオフセットを渡すことで、掘削範囲をプレイヤーの前方または足元に動的に配置します。

- **スコア管理**:
    - `DroppedItem`タグを持つオブジェクトと衝突すると、そのアイテムを破壊し、スコアを加算します。
    - UIの`Text`コンポーネントを更新して、現在のスコアを表示します。

### 設計の意図

- **柔軟な移動モード**: `MoveMode`を切り替えることで、2Dスクロール風のゲームプレイと、トップダウンビュー風のゲームプレイの両方に対応できる柔軟な設計になっています。
- **物理ベースの移動**: `Rigidbody`を使用することで、Unityの物理エンジンとの連携を容易にし、衝突判定などを自然に処理できます。
- **関心の分離**: プレイヤーの入力と移動ロジックを`PlayerController`に集約し、掘削ロジックを`Digger`に分離することで、コードの見通しを良くしています。

---

## 2. Digger

地形の掘削処理を担当するコンポーネントです。

### 主な機能

- **掘削範囲の定義**:
    - `BoxCollider`を掘削範囲（`diggingArea`）として利用します。この範囲は、外部のジェネレータ（例: `SphereTopDownGenerator`）によって設定されます。
    - `PlayerController`から`UpdateDiggingAreaTransform`メソッドが呼ばれることで、`diggingArea`の位置と回転がプレイヤーに追従します。

- **掘削の実行**:
    - マウスの左クリックをトリガーとして`Dig()`メソッドを実行します。
    - `Physics.OverlapBox`を使い、`diggingArea`内にある全ての`Collider`を検出します。
    - 検出した`Collider`からユニークな`VoxelChunk`を特定し、重複処理を防ぎます。
    - 各`VoxelChunk`の`DigVoxels()`メソッドを呼び出し、掘削範囲の`BoxCollider`を渡して、実際のボクセル破壊処理を委譲します。

### 設計の意図

- **汎用的な掘削ツール**: `Digger`は特定の形状に依存せず、`BoxCollider`で定義された任意の範囲を掘削できます。これにより、将来的に掘削範囲の形状を変更したり、異なるツールを実装したりすることが容易になります。
- **効率的なチャンク検出**: `Physics.OverlapBox`と`HashSet`を利用することで、掘削範囲内のチャンクを効率的に、かつ重複なく検出できます。
- **処理の委譲**: `Digger`は「どのチャンクを掘るか」を決定するだけで、実際のボクセル操作は`VoxelChunk`に任せています。これにより、各クラスの責務が明確になっています。

---

## 3. VoxelChunk

地形を構成する最小単位であるボクセルの集合（チャンク）を管理します。

### 主な機能

- **ボクセルデータ管理**:
    - `ChunkSize`で定義された3次元配列（`voxelTypes`, `voxelHPs`）で、各ボクセルの状態（空気か固体か）とHPを管理します。
    - `useTexture1Pattern`配列により、ボクセルごとのテクスチャパターンを保持します。

- **メッシュ生成**:
    - `GenerateMesh()`メソッドが、現在のボクセルデータに基づいて動的にメッシュを生成・更新します。
    - **最適化**: 隣接するボクセルが固体である場合、その間の面を描画しない「隠面消去」を実装し、描画負荷を軽減しています。
    - ボクセルのHPに応じて頂点カラーを変更し、ダメージ状態を視覚的に表現します。

- **掘削処理**:
    - `DigVoxels()`メソッドは、`Digger`から渡された`BoxCollider`の範囲内に含まれるボクセルを特定し、それらを空気（`voxelType = 0`）に変更します。
    - ボクセルが破壊されると、`DropItem()`メソッドを呼び出してアイテムを生成します。

- **アイテムドロップ**:
    - 破壊されたボクセルの位置に、`Rigidbody`を持つキューブ（`DroppedItem`）を生成します。

### 設計の意図

- **データ駆動の地形**: 地形の形状がボクセルデータ配列によって完全に定義されるため、セーブ・ロードや動的な地形変化の実装が容易です。
- **パフォーマンス**: チャンク単位でメッシュを管理し、隠面消去を行うことで、広大なボクセルワールドを効率的に描画できます。
- **カプセル化**: ボクセルに関する全てのロジック（データ、メッシュ生成、当たり判定、破壊処理）が`VoxelChunk`クラス内にカプセル化されており、他のクラスはボクセルの詳細を意識する必要がありません。

---

## 4. BaseCubePlacer

`VoxelChunk`をシーンに配置し、初期化するための抽象基底クラスです。

### 主な機能

- **チャンクの生成と配置**:
    - `CreateChunk()`ヘルパーメソッドを提供し、指定された位置に`VoxelChunk`を持つゲームオブジェクトを生成します。
    - チャンクの`GameObject`を自身の`transform`の子に設定し、階層構造を整理します。
    - `cubeSize`に基づいてチャンクのスケールを調整し、設計通りのサイズで表示されるようにします。

- **マテリアル設定**:
    - URP（Universal Render Pipeline）のTransparentシェーダーを使用するマテリアルを動的に生成し、`VoxelChunk`の`MeshRenderer`に設定します。
    - テクスチャを割り当て、アルファクリッピングを有効にすることで、半透明の表現を可能にしています。

- **初期化**:
    - `VoxelChunk`の`Initialize()`メソッドを呼び出し、テクスチャパターンやボクセルサイズなどの初期設定を渡します。

### 設計の意図

- **再利用可能なロジックの共通化**: チャンクの生成、スケーリング、マテリアル設定といった、あらゆる地形ジェネレータで必要となる共通処理を`BaseCubePlacer`に集約しています。
- **拡張性**: `Generate()`メソッドを`abstract`とすることで、このクラスを継承する派生クラス（例: `CubeTopDownPlacer`, `SphereSideScrollerGenerator`）に具体的な地形生成アルゴリズムの実装を強制しています。これにより、様々な形状の地形を簡単に追加できます。

---

## システム全体のフロー

1.  ゲーム開始時、`BaseCubePlacer`の派生クラス（例: `CubeTopDownPlacer`）が`Generate()`を実行し、複数の`VoxelChunk`をシーンに配置して地形を形成する。
2.  プレイヤーは`PlayerController`を通じてキャラクターを操作する。移動に応じて、`Digger`の掘削範囲も追従する。
3.  プレイヤーがマウスをクリックすると、`Digger`が掘削範囲内の`VoxelChunk`を検出する。
4.  `Digger`は検出した各`VoxelChunk`の`DigVoxels()`を呼び出す。
5.  `VoxelChunk`は指定された範囲のボクセルを破壊し、メッシュを更新して見た目を変更する。同時に`DroppedItem`を生成する。
6.  プレイヤーが`DroppedItem`に触れると、`PlayerController`がそれを検知してスコアを更新し、アイテムを消去する。

この一連の流れにより、動的な地形掘削とアイテム収集というコアなゲームサイクルが実現されています。
